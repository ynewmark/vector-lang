package org.vectorlang.compiler.compiler;

public enum OpCode {
    ADD, SUB, MULT, DIV, F_ADD, F_SUB, F_MULT, F_DIV, AND, OR, NEG, F_NEG, NOT,
    DEBUG, CONCAT, JMP, JIF, STORE, LOAD, ALLOC, INDEX, PUSH, PUSHI, PRINT
}
